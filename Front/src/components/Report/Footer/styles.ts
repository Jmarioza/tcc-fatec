import { StyleSheet } from '@react-pdf/renderer'

export const styles = StyleSheet.create({
  container: {
    display: 'flex',
    width: '100%',
    marginTop: 10,
  },

  text: {
    fontSize: 6,
    fontWeight: 300,
  },
})
